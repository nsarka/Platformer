#include "Toolbar.h"


Toolbar::Toolbar(SpriteSheet* levelSheet)
{

}

void Toolbar::Draw(SDL_Renderer* pRenderer)
{
	
	//SDL_RenderCopyEx(pRenderer, textureMap[id], &srcRect, &destRect, angle, 0, flip);
}

void Toolbar::Populate()
{
	//Fill toolbar with tiles for use
}
